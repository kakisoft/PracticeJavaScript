//javascript ：内部的に UTF-16？
//「吉野家」を 4文字と判定

// 昔の書き方
const isHoge = function(a) {
  return a === 'hoge';
};

// 今
const isHoge = a => {
  return a === 'hoge';
};

// 解読不能w
const isHoge = a => a === 'hoge';
